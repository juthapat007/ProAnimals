<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
     <%- include('../partials/link') %>
<title>ระบบจัดการลูกค้า</title>
</head>
<style>
    .hidden-row {
  display: none;
}
</style>
<body >
  
<div class="container ">
    <!-- Header Section -->

         <%- include('../partials/header_admin') %>
        <!-- Include your admin header here -->
<div class="container flex flex-col lg:flex-row justify-between shadow-custom items-center gap-4">

          <div>
            <br>
            <h1 class="text-2xl font-bold text-text mb-2 ">
                <i class="fas fa-users mr-3 text-primary2"></i>ระบบจัดการลูกค้า
            </h1>
            <p class="text-gray-600">จัดการข้อมูลและช่องทางการติดต่อลูกค้า</p>
          </div>
        
        <!-- Search Section -->
        <br><br>
        <div class="flex flex-col  lg:flex-row justify-between items-center gap-4 mb-6">
            
            <div class="relative flex-1 max-w-md"><br>
  <i class="fas fa-search absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
  
  <input type="text" 
       class="w-full form-info pl-10" 
       id="searchInput"
       placeholder="ค้นหา ชื่อ-นามสกุล">

</div>

        </div>
        </div>
        
        <!-- Search Results -->
        <ul id="resultList" class="mb-6"></ul>

        <!-- Form Section -->
        <form class="bg-gradient-to-br from-light-pink to-white p-6 rounded-2xl shadow-custom border border-primary/20 mb-8">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
                <div>
                    <label class="flex items-center gap-2 font-semibold text-text mb-2">
                        <i class="fas fa-hashtag text-primary2"></i>ลำดับ
                    </label>
                    <input class="w-full px-4 py-3 border-2 border-pink-200 rounded-xl focus:border-pink-400 focus:ring-2 focus:ring-pink-200 outline-none transition-all" 
                           type="text" 
                           id="cus_id" 
                           name="cus_id" 
                           placeholder="ลำดับ" 
                           readonly>
                </div>
                
                <div >
                    <label class="flex items-center gap-2 font-semibold text-text mb-2">
                        <i class="fas fa-user text-primary2"></i>ชื่อ-นามสกุล
                    </label>
                    <input class="w-full px-4 py-3 border-2 border-primary/30 rounded-custom focus:border-primary focus:ring-2 focus:ring-primary/20" 
                           type="text" 
                           id="cus_name" 
                           name="name" 
                           placeholder="ชื่อ-นามสกุล" 
                           required>
                </div>
                
                <div >
                    <label class="flex items-center gap-2 font-semibold text-text mb-2">
                        <i class="fas fa-envelope text-primary2"></i>Email
                    </label>
                    <input class="w-full px-4 py-3 border-2 border-primary/30 rounded-custom focus:border-primary focus:ring-2 focus:ring-primary/20" 
                           type="email" 
                           id="cus_email" 
                           name="email" 
                           placeholder="Email">
                </div>
                
                <div>
                    <label class="flex items-center gap-2 font-semibold text-text mb-2">
                        <i class="fas fa-phone text-primary2"></i>เบอร์โทร
                    </label>
                    <input class="w-full px-4 py-3 border-2 border-primary/30 rounded-custom focus:border-primary focus:ring-2 focus:ring-primary/20" 
                           type="tel" 
                           id="cus_phon" 
                           name="phone" 
                           placeholder="เบอร์โทร" 
                           required>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="flex flex-wrap gap-4 justify-center">
                <button type="button" 
                        id="addBtn" 
                        class="btn btn-add">
                    <i class="fas fa-plus"></i>เพิ่ม
                </button>
                                <button type="button" 
                        id="clearBtn" 
                        class="btn btn-cancel ">
                    <i class="fas fa-broom"></i>เคลียร์
                </button>
                <button type="button" 
                        id="deleteBtn" 
                        class="btn btn-delete" 
                        deleteBtn.disabled = false;>
                    <i class="fas fa-trash"></i>ลบ
                </button>

            </div>
        </form>


    <!-- Table Section -->
    <div class="bg-white rounded-2xl shadow-lg overflow-hidden" style="box-shadow: 0 10px 25px rgba(255, 154, 162, 0.15);">
        <div class="overflow-x-auto">
            <table class="w-full">
                <!-- Header -->
                <thead class="text-white">
                    <tr>
                        <th class="px-4 py-4 text-left font-semibold text-sm uppercase tracking-wider" style="background-color: #FF9AA2;">
                            <i class="fas fa-hashtag mr-2"></i>ลำดับ
                        </th>
                        <th class="px-4 py-4 text-left font-semibold text-sm uppercase tracking-wider" style="background-color: #FFB3BA;">
                            <i class="fas fa-user mr-2"></i>ชื่อ
                        </th>
                        <th class="px-4 py-4 text-left font-semibold text-sm uppercase tracking-wider" style="background-color: #FFCCCE;">
                            <i class="fas fa-phone mr-2"></i>เบอร์โทร
                        </th>
                        <th class="px-4 py-4 text-left font-semibold text-sm uppercase tracking-wider" style="background-color: #FFE6E7;">
                            <i class="fas fa-envelope mr-2"></i>Email
                        </th>
                        <th class="px-4 py-4 text-center font-semibold text-sm uppercase tracking-wider" style="background-color: #F1C5D0;">
                            <i class="fas fa-cogs mr-2"></i>จัดการข้อมูล
                        </th>
                    </tr>
                </thead>
               <tbody id="customerTableBody">
  <% customers.forEach(c => { %>
    <tr class="clickable-row"
        data-id="<%= c.cus_id %>"
        data-name="<%= c.cus_name %>"
        data-email="<%= c.cus_email %>"
        data-phon="<%= c.cus_phon %>">
      <td class="px-4 py-4"><%= c.cus_id %></td>
      <td class="px-4 py-4"><%= c.cus_name %></td>
      <td class="px-4 py-4"><%= c.cus_phon %></td>
      <td class="px-4 py-4"><%= c.cus_email %></td>
      
      <td class="px-4 py-4 text-center">
         <div class="btn" onclick="sendEmail('<%= c.cus_email %>', '<%= c.cus_name %>')">
    <i class="fas fa-envelope"></i>
  </div>
        <button class="btn bg-warning" onclick="goToPetList('<%= c.cus_id %>')">สัตว์เลี้ยง</button>
      </td>
    </tr>
  <% }) %>
</tbody>

            </table>
        </div>
    </div>
</div>
<script src="/js/mg_customer.js" type="module"></script>
<script>
// ฟังก์ชันไปหน้ารายการสัตว์เลี้ยง
function goToPetList(cusId) {
    if (cusId) {
        window.location.href = `/admin/add_pet?cus_id=${encodeURIComponent(cusId)}`;
    } else {
        alert("ไม่พบ cus_id");
    }
}

// ฟังก์ชันไปหน้าเพิ่มสัตว์เลี้ยง
function goToAddPet() {
    const cusId = document.getElementById("cus_id").value;
    if (cusId) {
        window.location.href = `/admin/add_pet?cus_id=${encodeURIComponent(cusId)}`;
    } else {
        alert("กรุณาเลือกลูกค้าก่อน");
    }
}

// ฟังก์ชันไปหน้าชำระเงิน
function Pay() {
    const cusId = document.getElementById("cus_id").value;
    if (cusId) {
        window.location.href = `/total_pay?cus_id=${encodeURIComponent(cusId)}`;
    } else {
        alert("กรุณาเลือกลูกค้าก่อน");
    }
}
function sendEmail(email, name) {
    if (!email) {
        alert("ไม่พบอีเมลของลูกค้า");
        return;
    }
    window.location.href = `mailto:${email}?subject=ติดต่อจากคลินิกไชยปราการสัตวแพทย์&body=เรียน คุณ${name} ตอนนี้คิวของคุณใกล้จะถึงแล้วนะคะ รบกวนเดินทางมารอที่ร้านได้เลยค่ะ ขอบคุณมากค่ะ 💚🐶🐱`;
}
document.addEventListener("DOMContentLoaded", function () {
    const searchInput = document.getElementById("searchInput"); // ✅ แก้ตรงนี้
    const tableBody = document.getElementById("customerTableBody");
    const allRows = Array.from(tableBody.querySelectorAll("tr"));

    function searchCustomer() {
        const keyword = searchInput.value.toLowerCase().trim();
        let found = false;

        allRows.forEach(row => {
            const customerName = row.querySelector("td:nth-child(2)").textContent.toLowerCase();
            if (customerName.includes(keyword)) {
                row.classList.remove("hidden-row");
                found = true;
            } else {
                row.classList.add("hidden-row");
            }
        });

        // แสดง/ซ่อน "ไม่พบลูกค้า"
        let noResults = document.getElementById("noResults");
        if (!noResults) {
            noResults = document.createElement("tr");
            noResults.id = "noResults";
            noResults.innerHTML = `
                <td colspan="5" class="text-center py-4 text-gray-500">
                    ไม่พบลูกค้า
                </td>`;
            tableBody.appendChild(noResults);
        }

        if (!found && keyword !== "") {
            noResults.classList.remove("hidden-row");
        } else {
            noResults.classList.add("hidden-row");
        }

        // ถ้าช่องค้นหาว่าง → แสดงข้อมูลทั้งหมด
        if (keyword === "") {
            allRows.forEach(row => row.classList.remove("hidden-row"));
            noResults.classList.add("hidden-row");
        }
    }

    // Event input
    searchInput.addEventListener("input", searchCustomer);
});




</script>


</body>
</html>
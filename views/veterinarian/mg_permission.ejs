<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>จัดการสิทธิ์</title>
 
  <%- include('../partials/link') %>
</head>
<body>
  <div class="container">
    
  <%- include('../partials/header_vet') %>
    <h2>รายการสิทธิ์การเข้าถึง</h2>
    <table border="1" cellpadding="8">
      <thead>
        <tr>
          <th>Email</th>
          <th>ชื่อ</th>
          <th>เบอร์โทร</th>
          <th>สิทธิ์ (access_type)</th>
          <th>การจัดการ</th>
        </tr>
      </thead>
      <tbody>
        <% if (permissions && permissions.length > 0) { %>
          <% permissions.forEach(p => { %>
            <tr>
              <td><%= p.access_email %></td>
              <td><%= p.cus_name %></td>
              <td><%= p.cus_phon %></td>
              <td><%= p.access_type %></td>
              
              <td>
                <!-- ฟอร์มแก้ไข -->
                <form action="/veterinarian/mg_permission/update/<%= p.count_users %>" method="POST">
                <select name="access_type" required>
                    <option value="customer" <%= p.access_type === "customer" ? "selected" : "" %>>Customer</option>
                    <option value="admin" <%= p.access_type === "admin" ? "selected" : "" %>>Admin</option>
                    <option value="veterinarian" <%= p.access_type === "veterinarian" ? "selected" : "" %>>Veterinarian</option>
                  </select>
                  <button type="submit">บันทึก</button>
                </form>
              </td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="5">ไม่มีข้อมูลสิทธิ์</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>ประวัติการรักษา</title>
    <%- include('../partials/link') %>
</head>
<body>
    <div class="container">
    <%- include('../partials/header_vet',{ vetId: vetId }) %>
     

 <div style="display: flex; justify-content: center;">
     <div class="flex items-center gap-2">
     <i class="fas fa-history text-xl text-primary2"></i>
    <strong class="text-2xl font-bold text-text">ประวัติการักษา</strong>
</div></div><br>

  <div class="overflow-x-auto shadow-lg rounded-lg">
  <table class="container shadow-sm w-full min-w-[800px] border-collapse text-left bg-white rounded-lg">
    <thead>
      <tr>
        <th class="tb-b1">รหัสการรักษา</th>
<th class="tb-b2">น้ำหนักสัตว์ (กก.)</th>
<th class="tb-b3">วันที่รักษา</th>
<th class="tb-b4">รายละเอียด</th>
<th class="tb-b6">รหัสสัตวแพทย์</th>
<th class="tb-b7">สถานะ</th>
<th class="tb-b8">วิธีชำระเงิน</th>
      </tr>
    </thead>
    <tbody>
      <% treatments.forEach(t => { %>
        <tr class="clickable-row hover:bg-gray transition-colors">
          <td class="px-4 py-3 border-b border-gray-200 text-center"><%= t.treatment_id %></td>
          <td class="px-4 py-3 border-b border-gray-200 text-center"><%= t.pet_weight_kg %></td>
          <td class="px-4 py-3 border-b border-gray-200 text-center"><%= t.treatment_date.toISOString().split('T')[0] %></td>
          <td class="px-4 py-3 border-b border-gray-200 text-left"><%= t.treatment_details %></td>
          <td class="px-4 py-3 border-b border-gray-200 text-center"><%= t.vet_id %></td>

      <td class="px-4 py-3 border-b border-gray-200 text-center <%= t.pay_status === 'ชำระแล้ว' ? 'bg-green-200 text-green-800 font-semibold' : t.pay_status === 'ค้างชำระ' ? 'bg-red-200 text-red-800 font-semibold' : '' %>">
        <%= t.pay_status %>
      </td>

          <td class="px-4 py-3 border-b border-gray-200 text-right"><%= t.payment %> ฿</td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

  </div>
</body>
</html>

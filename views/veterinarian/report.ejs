<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายงานการเข้ารับบริการ</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;700&display=swap" rel="stylesheet">
  <%- include('../partials/link') %>
  <style>
   

   
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
     <%- include('../partials/header_vet') %>
    <div class="flex flex-wrap gap-4">
      <strong class="text-xl">
        <i class="fas fa-chart-bar text-primary2"></i>
        ระบบรายงานสัตวแพทย์
      </strong><br>
      
    </div><p>ข้อมูลสถิติการเข้ารับบริการ</p><br>

    <div class="content">
      <!-- Report Title -->



      <!-- Filter Section -->
      <div class="filter-section">
        <div class="filter-label">
          เลือกช่วงเวลาที่ต้องการดู
        </div>
        <div class="filter-buttons" id="filterButtons">
          <button class="filter-btn <%= type === 'week' ? 'active' : '' %>" data-type="week">
            <i class="fas fa-calendar-week"></i>
            รายสัปดาห์
          </button>
          <button class="filter-btn <%= type === 'month' ? 'active' : '' %>" data-type="month">
            <i class="fas fa-calendar-alt"></i>
            รายเดือน
          </button>
          <button class="filter-btn <%= type === 'year' ? 'active' : '' %>" data-type="year">
            <i class="fas fa-calendar"></i>
            รายปี
          </button>
        </div>
      </div>

      <!-- Chart -->
      <div class="chart-container">
        <canvas id="chartCanvas"></canvas>
      </div>
    </div>
  </div>

  <script>
    // Filter buttons functionality
    document.querySelectorAll('#filterButtons button').forEach(btn => {
      btn.addEventListener('click', () => {
        const type = btn.getAttribute('data-type');
        window.location.href = '?type=' + type;
      });
    });

    // Chart configuration with theme colors
    const ctx = document.getElementById('chartCanvas').getContext('2d');
    
    // Create gradient
    const gradient = ctx.createLinearGradient(0, 0, 0, 400);
    gradient.addColorStop(0, '#b2dcfe');
    gradient.addColorStop(1, 'rgba(255, 183, 178, 0.3)');

    new Chart(ctx, {
      type: 'bar',
      data: {
        labels: <%- JSON.stringify(labels) %>,
        datasets: [{
          label: 'จำนวนการเข้ารับบริการ',
          data: <%- JSON.stringify(data) %>,
          backgroundColor: gradient,
          borderColor: '#9FA9F0',
          borderWidth: 2,
          borderRadius: 8,
          borderSkipped: false,
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: true,
        plugins: {
          legend: {
            display: true,
            labels: {
              color: '#333333',
              font: {
                family: 'Kanit',
                size: 14
              }
            }
          },
          tooltip: {
            backgroundColor: '#b2dcfe)',
            titleColor: '#ffffff',
            bodyColor: '#ffffff',
            borderColor: '#b2dcfe',
            borderWidth: 1,
            cornerRadius: 8,
            font: {
              family: 'Kanit'
            }
          }
        },
        scales: {
          y: {
            beginAtZero: true,
            title: {
              display: true,
              text: 'จำนวน (ครั้ง)',
              color: '#333333',
              font: {
                family: 'Kanit',
                size: 14,
                weight: 'bold'
              }
            },
            ticks: {
              color: '#333333',
              font: {
                family: 'Kanit'
              }
            },
            grid: {
              color: 'rgba(255, 154, 162, 0.1)'
            }
          },
          x: {
            title: {
              display: true,
              text: 'ช่วงเวลา',
              color: '#FF9AA2',
              font: {
                family: 'Kanit',
                size: 14,
                weight: 'bold'
              }
            },
            ticks: {
              color: '#333333',
              font: {
                family: 'Kanit'
              }
            },
            grid: {
              color: 'rgba(255, 154, 162, 0.1)'
            }
          }
        },
        animation: {
          duration: 1000,
          easing: 'easeOutQuart'
        }
      }
    });
  </script>
</body>
</html>
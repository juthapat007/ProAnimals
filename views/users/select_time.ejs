<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>จองคิวบริการ - เลือกเวลา</title>
  <style>
    .service-info {
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 16px;
      margin: 16px 0;
    }
    
    .service-info h3 {
      margin: 0 0 8px 0;
      color: #2d3748;
    }
    
    .service-info p {
      margin: 4px 0;
      color: #4a5568;
    }
    
    .time-slots {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: 12px;
      margin: 20px 0;
    }
    
    .time-slot {
      border: 2px solid #e2e8f0;
      border-radius: 8px;
      padding: 16px;
      text-align: center;
      cursor: pointer;
      transition: all 0.2s ease;
      background: white;
    }
    
    .time-slot.available {
      border-color: #48bb78;
      background: #f0fff4;
    }
    
    .time-slot.available:hover {
      border-color: #38a169;
      background: #c6f6d5;
      transform: translateY(-2px);
    }
    
    .time-slot.selected {
      border-color: #3182ce;
      background: #bee3f8;
      transform: translateY(-2px);
    }
    
    .time-slot.booked {
      border-color: #e53e3e;
      background: #fed7d7;
      cursor: not-allowed;
      opacity: 0.6;
    }
    
    .time-main {
      font-size: 18px;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 4px;
    }
    
    .time-end {
      font-size: 14px;
      color: #718096;
    }
    
    .time-duration {
      font-size: 12px;
      color: #a0aec0;
      margin-top: 4px;
    }
    
    .legend {
      display: flex;
      gap: 20px;
      margin: 16px 0;
      font-size: 14px;
    }
    
    .legend-item {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .legend-color {
      width: 16px;
      height: 16px;
      border-radius: 4px;
      border: 2px solid;
    }
    
    .legend-available {
      border-color: #48bb78;
      background: #f0fff4;
    }
    
    .legend-booked {
      border-color: #e53e3e;
      background: #fed7d7;
    }
    
    .legend-selected {
      border-color: #3182ce;
      background: #bee3f8;
    }

    /* Mobile Responsive Styles */
    @media (max-width: 480px) {
      .time-slots {
        grid-template-columns: 1fr 1fr;
        gap: 8px;
      }
      
      .time-slot {
        padding: 12px 8px;
      }
      
      .time-main {
        font-size: 16px;
      }
      
      .time-end {
        font-size: 13px;
      }
      
      .legend {
        flex-direction: column;
        gap: 8px;
      }
    }

    @media (max-width: 359px) {
      .time-slots {
        grid-template-columns: 1fr;
      }
    }
  </style>
  <%- include('../partials/link') %>
</head>
<body>
  <div class="container">
    <%- include('../partials/header') %>

    <div class="progress">
      <div class="progress-step">
        <div class="progress-circle">1</div>
        <span>ข้อมูลสัตว์</span>
      </div>
      <div class="progress-step">
        <div class="progress-circle">2</div>
        <span>เลือกบริการ-วัน</span>
      </div>
      <div class="progress-step active">
        <div class="progress-circle">3</div>
        <span>เวลา</span>
      </div>
      <div class="progress-step">
        <div class="progress-circle">4</div>
        <span>ยืนยัน</span>
      </div>
      <div class="progress-step">
        <div class="progress-circle">5</div>
        <span>เสร็จสิ้น</span>
      </div>
    </div>

    <form action="/users/confirm" method="post" id="bookingForm">
      <p><strong>วันที่:</strong> <%= date %></p>
      <input type="hidden" name="date" value="<%= date %>">
      
      <!-- Service Information -->
      <div class="service-info">
        <h3><%= service.service_type %></h3>
        <p><strong>ราคา:</strong> <%= service.service_price %> บาท</p>
        <p><strong>ระยะเวลา:</strong> <%= Math.floor(serviceDurationMinutes / 60) %> ชั่วโมง <%= serviceDurationMinutes % 60 %> นาที</p>
      </div>

      <div style="text-align: left;">
        <h2>เลือกเวลา <span style="color: red;">*</span></h2>
      </div>

      <!-- Legend -->
      <div class="legend">
        <div class="legend-item">
          <div class="legend-color legend-available"></div>
          <span>ว่าง</span>
        </div>
        <div class="legend-item">
          <div class="legend-color legend-booked"></div>
          <span>ถูกจองแล้ว</span>
        </div>
        <div class="legend-item">
          <div class="legend-color legend-selected"></div>
          <span>เลือกแล้ว</span>
        </div>
      </div>

      <input type="hidden" name="cus_id" value="<%= cus_id %>">
      <input type="hidden" name="pet_id" value="<%= pet_id %>">
      <input type="hidden" name="service_id" value="<%= service_id %>">
      <input type="hidden" name="time" id="selectedTimeInput" required />

      <!-- Time Slots -->
      <div class="time-slots">
        <% allSlots.forEach(slot => { %>
          <% 
            const isAvailable = availableSlots.includes(slot);
            const slotEndTime = (() => {
              const startMinutes = Math.floor(parseInt(slot.split(':')[0])) * 60 + parseInt(slot.split(':')[1]);
              const endMinutes = startMinutes + serviceDurationMinutes;
              const hours = Math.floor(endMinutes / 60);
              const mins = endMinutes % 60;
              return `${hours.toString().padStart(2, '0')}:${mins.toString().padStart(2, '0')}`;
            })();
          %>
          
          <div 
            class="time-slot <%= isAvailable ? 'available' : 'booked' %>"
            data-time="<%= slot %>"
            data-available="<%= isAvailable %>"
          >
            <div class="time-main"><%= slot %></div>
            <div class="time-end">ถึง <%= slotEndTime %></div>
            <div class="time-duration">
              (<%= Math.floor(serviceDurationMinutes / 60) %>:<%= String(serviceDurationMinutes % 60).padStart(2, '0') %> ชม.)
            </div>
          </div>
        <% }); %>
      </div>

      <% if (availableSlots.length === 0) { %>
        <div style="text-align: center; padding: 20px; background: #fed7d7; border-radius: 8px; color: #c53030;">
          <p><strong>ขออภัย ไม่มีช่วงเวลาว่างสำหรับบริการนี้ในวันที่เลือก</strong></p>
          <p>กรุณาเลือกวันอื่น หรือบริการอื่น</p>
        </div>
      <% } %>

      <button type="submit" class="button" style="width: 100%; height: 60px;" id="nextButton" disabled>
        ถัดไป
      </button><br><br>
      
      <button type="button" class="button btn-back" onclick="history.back()" style="width: 100%;">
        <i class="fas fa-arrow-left"></i> ย้อนกลับ
      </button>
    </form>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      let selectedSlot = null;
      const nextButton = document.getElementById('nextButton');
      const selectedTimeInput = document.getElementById('selectedTimeInput');

      // Handle time slot selection
      document.querySelectorAll('.time-slot[data-available="true"]').forEach(slot => {
        slot.addEventListener('click', function() {
          // Remove previous selection
          document.querySelectorAll('.time-slot').forEach(s => s.classList.remove('selected'));
          
          // Add selection to current slot
          this.classList.add('selected');
          
          // Update form data
          selectedSlot = this.getAttribute('data-time');
          selectedTimeInput.value = selectedSlot;
          nextButton.disabled = false;
        });
      });

      // Form validation
      document.getElementById('bookingForm').addEventListener('submit', function(e) {
        if (!selectedTimeInput.value.trim()) {
          e.preventDefault();
          alert('กรุณาเลือกช่วงเวลาด้วยค่ะ');
        }
      });

      // Disable form if no available slots or error
      <% if (typeof availableSlots !== 'undefined' && availableSlots.length === 0) { %>
        nextButton.style.display = 'none';
      <% } %>
      
      <% if (typeof error !== 'undefined' && error) { %>
        nextButton.style.display = 'none';
      <% } %>
    });
  </script>
</body>
</html>
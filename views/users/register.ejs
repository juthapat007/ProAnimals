<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>สมัครสมาชิก | ไชยปราการสัตวแพทย์</title>
  <%- include('../partials/link') %>
</head>
<body>
  <div class="container max-w-2xl bg-white rounded-2xl shadow-lg p-6 ">
    <div class="header text-text text-2xl">
      <h1><i class="fas fa-user-plus text-primary2"></i> สมัครสมาชิก</h1>
    </div>

    <% if (error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>

    <% if (success) { %>
      <div class="success-message">
        <%= success %>
        <br><br>
        <a href="/" class="btn-login">เข้าสู่ระบบ</a>
      </div>
    <% } %>

    <% if (!success) { %>
    <form class="register-form" action="/users/register" method="post">
      <div class="form-group">
        <strong class="text-lg" for="name">ชื่อ-นามสกุล <span class="required" style="color: red;">*</span></strong>
        <input 
          style="border-color:#f5b2b7"
          class="form-input"
          type="text" 
          name="name" 
          id="name" 
          value="<%= old.name || '' %>"
          required 
          placeholder="กรอกชื่อ-นามสกุล"
        />
      </div>

      <div class="form-group">
        <strong class="text-lg" for="phone">เบอร์โทรศัพท์ <span class="required" style="color: red;">*</span></strong>
        <input 
        style="border-color:#f5b2b7"
          type="tel" 
          class="form-input"
          name="phone" 
          id="phone" 
          pattern="[0-9]{10}" 
          value="<%= old.phone || '' %>"
          required 
          placeholder="0912345678"
        />
        <!-- <p class="text-sm">รูปแบบ: 0912345678 (10 หลัก)</p> -->
      </div>

      <div class="form-group">
        <strong class="text-lg"  for="email">อีเมล <span class="required" style="color: red;">*</span></strong>
        <input 
        style="border-color:#f5b2b7"
          type="email" 
          name="email" 
          class="form-input"
          id="email"
          value="<%= old.email || '' %>"
          required
          placeholder="example@email.com"
        />
      </div>

      <div class="form-group">
        <strong class="text-lg"  for="password">รหัสผ่าน <span class="required" style="color: red;">*</span></strong>
        <input 
        style="border-color:#f5b2b7"
          type="password" 
          name="password" 
          class="form-input"
          id="password" 
          minlength="6" 
          required 
          placeholder="อย่างน้อย 6 ตัวอักษร"
        />
        <small>รหัสผ่านต้องมีความยาวอย่างน้อย 6 ตัวอักษร</small>
      </div>

      <div class="form-group">
        <strong class="text-lg"  for="confirm_password">ยืนยันรหัสผ่าน <span class="required" style="color: red;">*</span></strong>
        <input
        style="border-color:#f5b2b7"
        type="password"
        name="confirm_password"
        id="confirm_password"
        class="form-input"
        required
        placeholder="กรอกรหัสผ่านอีกครั้ง"
/>

      </div>

      <button type="submit" class="btn-primary text-xl" style="width: 100%; height: 60px;">
        <i class="fas fa-user-plus"></i> สมัครสมาชิก
      </button>
    </form>
    <% } %>

    <div class="login-link" style="height: 50px;">
      <br><span class="text-lg">มีบัญชีอยู่แล้ว? <a href="/" style="color: #FF9AA2;" ><i class="fas fa-sign-out-alt text-lg"></i>  เข้าสู่ระบบที่นี่</a></span>
    </div>
  </div>

  <script>
    
    // ตรวจสอบการยืนยันรหัสผ่าน
    document.getElementById('confirm_password').addEventListener('input', function() {
      const password = document.getElementById('password').value;
      const confirmPassword = this.value;
      
      if (password !== confirmPassword && confirmPassword.length > 0) {
        this.setCustomValidity('รหัสผ่านไม่ตรงกัน');
      } else {
        this.setCustomValidity('');
      }
    });

    // ตรวจสอบรูปแบบเบอร์โทร
    document.getElementById('phone').addEventListener('input', function() {
      const phone = this.value;
      if (phone.length > 0 && !/^[0-9]{10}$/.test(phone)) {
        this.setCustomValidity('เบอร์โทรศัพท์ต้องเป็นตัวเลข 10 หลัก');
      } else {
        this.setCustomValidity('');
      }
    });
  </script>

</body>
</html>